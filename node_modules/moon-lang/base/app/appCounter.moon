id => {
  type: "app",
  name: id,
  state: 0,
  render: size => state =>
    {
      type: "box"
      pos: [0 0]
      size: size
      cursor: "pointer"
      unselectable: 1
      onClick: do => end => (do "setState" (add 1 state) then => (end 0))
      children: [
        (con "ClickCount: " (nts state))
      ]
    }
}
