hex =>
  (for 2 (len hex) "0x" i => r =>
    h = (slc hex i (add i 1))
    (if (cmp h "1") (con r "0001")
    (if (cmp h "2") (con r "0010")
    (if (cmp h "3") (con r "0011")
    (if (cmp h "4") (con r "0100")
    (if (cmp h "5") (con r "0101")
    (if (cmp h "6") (con r "0110")
    (if (cmp h "7") (con r "0111")
    (if (cmp h "8") (con r "1000")
    (if (cmp h "9") (con r "1001")
    (if (cmp h "a") (con r "1010")
    (if (cmp h "b") (con r "1011")
    (if (cmp h "c") (con r "1100")
    (if (cmp h "d") (con r "1101")
    (if (cmp h "e") (con r "1110")
    (if (cmp h "f") (con r "1111")
    (con r "0000")))))))))))))))))
